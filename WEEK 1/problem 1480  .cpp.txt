#include <vector>

class Solution {
public:
    std::vector<int> runningSum(std::vector<int>& nums) {
        std::vector<int> result;
        int current_sum = 0;

        for (int num : nums) {
            current_sum += num;       
            result.push_back(current_sum); 

        return result;
    }
};